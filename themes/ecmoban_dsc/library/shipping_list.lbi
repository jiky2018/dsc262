<ul>
    <!-- {foreach from=$shipping_list item=shipping name=shipping_name} -->
    <!-- {if $shipping.shipping_code neq 'cac'} -->
    <li class="mode-tab-item shopping-list-checked {if $shipping_type eq 0 && $tmp_shipping_id == $shipping.shipping_id}item-selected{/if}" data-ruid="{$ru_id}" data-type="0" data-shipping="{$shipping.shipping_id}">
        {$shipping.shipping_name}
    </li>
    <!-- {/if} -->
    <!-- {/foreach} -->
</ul>